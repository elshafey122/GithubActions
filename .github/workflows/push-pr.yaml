on:
  push:
    branches:
      - master
      - "release/**"
    paths:
      - "src/**"
jobs:
  test:
    runs-on : ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: Run tests
        run: npm test
