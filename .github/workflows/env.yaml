on: 
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: set api key
        run:  echo "API_KEY=1234" >> $GITHUB_ENV
      - name: use api key
        run: echo "using api key $API_KEY"
      - name: use api key2 
        run: |
          echo "::add-mask::$API_KEY"
          echo "using api key2 $API_KEY"
        
          
